Run start.bat (for windows) to start the server as a docker container.

Then open the index.html inside the'dockerJSClient' to test the server.

If you run server by 'java -jar..' command on localhost, then open the index.html inside the 'localJSClient' to test the server.